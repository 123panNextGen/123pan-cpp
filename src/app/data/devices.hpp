#pragma once

#include <string>
#include <vector>

namespace app {

/// 所有设备类型列表（用于随机设备指纹伪装）。
extern const std::vector<std::string> ALL_DEVICE_TYPES;

/// 所有操作系统版本列表（用于随机设备指纹伪装）。
extern const std::vector<std::string> ALL_OS_VERSIONS;

}  // namespace app
