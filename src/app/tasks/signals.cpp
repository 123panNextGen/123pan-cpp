// This file exists so that CMake's AUTOMOC can find and process
// the Q_OBJECT classes declared in signals.hpp.
// The actual implementation of signals is inline in the header.
#include "app/tasks/signals.hpp"
